# element-demo
Demo element for diskimage-builder

This element, for use with diskimage-builder, will modify the MOTD on any image it is included with

## Usage
	export ELEMENTS_PATH=$ELEMENTS_PATH:`pwd`/elements
	disk-image-create [ubuntu|fedora|centos] demo
	
